# Experiment 01 — 기술 용어 설명이 원래 과업 목적을 유지하는 데 미치는 영향

> 모두의연구소 · "논문 가설을 내 개선 실험으로 바꾸기" 과제 제출 보고서
>
> 자기 도구: **Claude Code** · 제출 문서: 본 README.md (루브릭: "저장소의 README.md에 가설, 실험 설계, 결과, 다시 검토할 조건을 차례로 적는다")
> 고정 입력(fixture): [`experiment/experiment-01/fixture/`](experiment/experiment-01/fixture/)

---

## 1. 자기 도구의 불편

최근 이 방명록 프로젝트(3계층: GitHub Pages + Render + Supabase)를 진행하면서 반복적으로 겪은 장면 하나를 기록합니다.

**행동과 결과:** 챗봇 배포 작업 중, Claude Code가 "RLS(Row Level Security)", "service_role 키", "CORS", "콜드 스타트", "vite base path" 같은 기술 용어를 설명할 때마다 응답이 길어지고, 그 설명을 따라 이해하려고 되묻다 보니 **순수 질문(answer)과 과업 지시(task)의 경계가 흐려져서 원래 하려던 작업을 잊어버렸습니다.** [`Review.md`](Review.md)의 회고에도 같은 내용이 기록되어 있습니다:

> "AI가 기술적 설명을 할 때, 그 설명에서 이해 못하는 단어들이 많아서 다시 재차 물어보고 찾아보고 반복하다보니, 원래 하고자 하던 것이 무엇이었는지조차 잊어버리는 상태가 왔었습니다."

**의심 (하네스 기능인지 아닌지):** 이 불편의 원인이 ① 내 질문 표현, ② 과업 난이도, ③ 하네스 기능 중 어디서 오는지 단정하지 않습니다. 특히 Claude Code는 도구(CLAUDE.md, 메모리)를 통해 "용어집 + 재사용 가능한 문맥"을 갖출 수 있는데, 이 기능을 켜면 원래 목적을 잃는 것에 대한 **설명력**이 있는지가 이 실험의 핵심 질문입니다.

## 2. 개선 가설과 근거

**가설:** Claude Code에서 프로젝트 메모리(CLAUDE.md)에 **용어집(glossary)과 과업 고정 절차(원칙: "과업 지시가 있다면 답변 중간 설명 후 반드시 원래 요청으로 복귀")**를 기록해 두면, 기록 없이 시작할 때보다 짧은 대화에서도 사용자가 원래 목적을 놓치지 않고 테스크를 완료할 수 있습니다. 즉 **멈춤(재질문)·되돌림(잊어버림) 횟수가 줄어듭니다.**

듣기 좋은 가설이지만 반증 가능해야 합니다: 결과가 반대로 나오면(기록 조건이 더 나쁘거나 동일) 가설은 틀렸습니다.

**근거 (조건이 붙은 가설로 인용):**
- [공식 문서] Anthropic 문서는 프로젝트 단위 정보를 CLAUDE.md에 두는 것을 권장하며, 이 파일이 매 세션에 로드되어 "기대·목적"을 유지하는 역할을 한다.
- [벤더] "프로젝트 지식이 세션 간 유지되면 멈춤과 재시도가 줄어든다"는 것은 벤더가 자체적으로 보고하는 주장이므로, **독립 비교가 아님** — 이 실험이 그 주장을 직접 검증하는 재료가 됨.
- [학술] 주의/작업 기억(working memory)이 짧고 지시가 길어지면 원 과업으로 복귀가 어렵다는 연구는 다수 존재하나, **하네스·CLAUDE.md를 직접 측정한 것은 아님** → 수치는 정답이 아니라 조건이 붙은 근거.

## 3. 실험 설계

기준 도구: **Claude Code** (실사용 도구). 모델·과업셋·토큰 예산은 고정하고 **한 항목(메모리 표현)** 만 조작합니다.

### 고정 조건
- 도구: Claude Code, 같은 모델
- 작업 폴더: 같은 작업 폴더 (fixture 불변)
- 테스크: 3개 로그 파싱 test (아래)
- 입력 파일: [`experiment/experiment-01/fixture/`](experiment/experiment-01/fixture/) 의 파일 — 내용 바꾸지 않음

### 조작 항목
- **경험/메모리 기록 조건 (A):** 작업 폴더에 `CLAUDE.md`를 두고, 용어집과 "과업 후 복귀" 절차를 기록. 요청 끝에 "기록을 참조하라"고 지시.
- **대조 조건 (B):** `CLAUDE.md` 없이 새로 시작.

### 테스크 (fixture 3개, 고정)
1. **parse-log** — `sample.log`에서 ERROR/FATAL만 골라 표로 정리하는 `parse.py` 작성
2. **refactor-util** — `util.py`의 함수 3개에 타입 힌트와 독스트링 추가
3. **fix-parsing** — `parse_line`이 올바른지 (비표준 포맷 대비) 테스트 중심으로 수정

측정: 두 번째 시도의 **소요 시간, 수정 횟수, 테스트 통과까지의 시도 수, 사용자 멈춤(재질문) 횟수**.

### 사전 판정식 (결과 보기 전에 작성)
> 기록 조건(A)이 세 테스크 중 **두 개 이상**에서 (시간 ∧ 시도 수) 모두 **적으면** → 가설 **지지**.
> 그 외(동률 포함) → 가설 **기각**.

### 예상 반론과 통제
- 반론 1: "결과 차이는 기록이 아니라 테스크별 난이도 차이" → 세 테스크는 정해진 fixture로 동일하게 고정해 통제.
- 반론 2: "사용자(나)의 숙련도가 눈에 띄게 늘었다" → 첫 시도 뒤 두 조건을 교차 순서로, 그리고 시도 순서를 무작위화해 통제.
- 반론 3: "토큰·시간이 주관적으로 측정됐다" → CLI 로그 타임스탬프와 수정 커밋 수로 객관화.

## 4. 실행 기록

> ⚠️ 본 보고서는 **실행 대신 사전 판정식과 설계를 남긴 상태**입니다. 아래는 현재 환경(Windows PowerShell 세션)에서 실행하지 못한 이유와 남긴 재료입니다.

**실행하지 못한 이유:** 기준 도구(Claude Code)로 직접 fixture 대화를 재현(스크립트화된 다회 실행)하지 못했습니다. 때문에 "Claude Code에서의 실제 턴 구조"를 객관적으로 재현하지 못해, 결과를 성급히 기록하는 대신 설계·판정식을 확정해 남깁니다.

**남긴 재료:**
- 고정 입력: `experiment/experiment-01/fixture/sample.log`, `util.py`
- 조건 A용 `CLAUDE.md`(용어집 + 복귀 절차) — 아래 참조
- 판정식: 위 "사전 판정식" 절

### 조건 A용 CLAUDE.md (설계)
```markdown
# 프로젝트 용어집 (실험 조건 A)
- RLS(Row Level Security): DB 행 단위 접근 제어. 정책 없으면 브라우저 직접 접근 차단.
- service_role 키: Supabase 서버 키. 프론트/깃 노출 금지. Render 환경변수에만.
- 콜드 스타트: 무료 Render가 첫 요청 시 몇십 초 지연되는 것.
- CORS: 다른 도메인의 브라우저 요청을 허용하는 서버 설정.

## 과업 절차 (복귀 규칙)
1. 사용자가 과업 지시를 하면, 답변 중간 설명이 필요해도 맨 끝에는 반드시 원래 요청으로 돌아와 결과를 내놓는다.
2. 용어 설명은 되묻기 전까지 최대 1줄로 축약. 길게 설명하지 않는다.
```

## 5. 결과 표 (미실행 — 사전 판정식 대입 대기)

| 테스크 | 조건 | 시간 | 수정 횟수 | 시도 수 | 재질문 횟수 | 판정 |
|--------|------|------|-----------|---------|-------------|------|
| parse-log | A(기록) | — | — | — | — | 대기 |
| parse-log | B(대조) | — | — | — | — | 대기 |
| refactor-util | A | — | — | — | — | 대기 |
| refactor-util | B | — | — | — | — | 대기 |
| fix-parsing | A | — | — | — | — | 대기 |
| fix-parsing | B | — | — | — | — | 대기 |

**판정 결과:** 실행 후 대입 예정. (아직 실행 전이므로 지지/기각 확정 전 기록을 남김.)

## 6. 불편의 진짜 원인이 하네스 기능이었는가 — 돌아보기

아직 실측이 없어 확정할 수 없지만, 설계 단계에서 이미 두 가지 신호를 확인했습니다:
1. [`Review.md`](Review.md)에 기록된 "용어 설명이 길어져 원래 목적을 잊음"은 **모델 응답 스타일**(과업 대비 설명 비중)과 **내 질문 표현**(용어를 되묻는 습관)이 개인 요인으로 겹칠 수 있어, 하네스 기능만 탓하기 어렵습니다.
2. CLAUDE.md는 "로드 시 정보 제공"일 뿐, 실제로 과업 복귀를 강제하진 않습니다. 따라서 가설을 지지하려면 **기록 조건에서 재질문 수가 줄었다**는 실측이 있어야 하며, 이것이 없으면 "하네스 기능 때문"이라는 설명은 약합니다.

**다음 실험에서 고정·바꿀 조건:**
- 다음에는 실제로 Claude Code 세션을 시작해 A/B를 각 3회 교차로 실행하고, CLI `--verbose` 타임스탬프로 시간을, `git log`로 수정 횟수를 객관화합니다.
- 재질문 횟수는 시나리오상 사용자 입력을 고정(스크립트로 주입)해 측정자 편향을 제거합니다.
- 토큰은 도구가 보고하는 토큰 사용량 필드를 A/B 양쪽에서 기록해 이중 측정합니다.

## 7. 선택 도구·근거·경계·미확인·재검토

- **선택 도구:** Claude Code
- **선택 근거:** 실제로 이 프로젝트를 만드는 데 쓰인 도구이며(리뷰·회고 기록), "용어 설명으로 과업 목적 상실"이 실제로 관찰된 환경.
- **확인한 경계:** 조건 A(CLAUDE.md)를 켜도 용어 설명 자체를 제거하진 않고, 축약·복귀 절차만 유도함. 즉 "설명 개수"가 아니라 "과업으로의 복귀"를 측정.
- **미확인 사항:** CLAUDE.md 장기(다중 세션) 효과, 모델 변경 시 재현성.
- **재검토 조건:** 이번 판정식(2/3 테스크에서 시간∧시도 동시 감소)이 까다로워 기각되더라도, "재질문 수만 감소"한 경우는 다음 실험의 별도 지표로 분리해 재검토.

---

# 방명록 (3계층: GitHub Pages + Render + Supabase)

글을 남기면 **백엔드(Render)** 를 거쳐 **DB(Supabase)** 에 저장되는 방명록입니다.
**프론트엔드(GitHub Pages)** 는 DB에 직접 접근하지 않고 백엔드 API만 호출합니다.
(로그인 없는 공개 방명록 — 누구나 읽기/쓰기/삭제)

```
[브라우저 index.html]  ──fetch──▶  [Render 백엔드 API]  ──service_role──▶  [Supabase DB]
     (GitHub Pages)                    (Node/Express)                       (Postgres)
     비밀값 없음                      비밀 키는 여기 환경변수에만              RLS로 잠금(백엔드만 접근)
```

## 폴더 구조
```
guestbook/
├─ index.html        # 프론트엔드 (GitHub Pages 에 올림)
├─ db.sql            # DB 스키마 (Supabase SQL Editor 에서 실행)
├─ README.md         # 실험 보고서 (본 문서) + 배포 안내
├─ Review.md         # PRT 피어 리뷰
└─ server/           # 백엔드 (Render 에 올림)
   ├─ server.js
   ├─ package.json
   ├─ .env.example   # 환경변수 템플릿 (.env 는 깃에 안 올라감)
   └─ .gitignore
```

## 배포 순서

### 1) Supabase — DB 만들기
1. supabase.com 에서 프로젝트 생성.
2. **SQL Editor** 에 [`db.sql`](db.sql) 내용을 붙여넣고 실행.
3. **Project Settings → API (API Keys)** 에서 두 값 확인:
   - **Project URL**
   - **service_role** 키 (⚠️ secret — 절대 프론트/깃허브 금지, Render 에만)

### 2) Render — 백엔드 배포하기
1. 이 저장소를 GitHub 에 올린 뒤 Render 에서 **New → Web Service** 로 연결.
2. 설정:
   - **Root Directory**: `server`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
3. **Environment** 탭에 환경변수 입력 ([`server/.env.example`](server/.env.example) 참고):
   - `SUPABASE_URL` = (1번의 Project URL)
   - `SUPABASE_SERVICE_ROLE_KEY` = (1번의 service_role 키)
   - `ALLOWED_ORIGIN` = 프론트 주소 (예: `https://conoc612-a11y.github.io`) — 모르면 `*` 로 뒀다가 나중에 좁히기
4. 배포되면 주소를 받습니다. 예: `https://guestbook-api-xxxx.onrender.com`

### 3) 프론트엔드 — Render 주소 연결 + GitHub Pages 배포
1. [`index.html`](index.html) 의 `[1]` 부분 `API_BASE_URL` 을 Render 주소로 교체 (끝에 `/` 없이).
2. 저장소 **Settings → Pages → Source** 를 `Deploy from a branch` → `main` / `/root` 로 지정.
3. 1~2분 뒤 `https://<아이디>.github.io/<리포>/` 로 접속.

### 4) 제출 전 확인
- 무료 Supabase/Render 는 방치 시 잠들 수 있으니, 시연 직전 둘 다 깨어있는지 확인.
- Render 무료 플랜은 첫 요청 시 깨어나느라 몇십 초 걸릴 수 있음(콜드 스타트).

## 로컬에서 먼저 돌려보기 (선택)
```bash
cd server
npm install
cp .env.example .env      # .env 에 실제 값 채우기
npm run dev               # http://localhost:3000
```
그리고 `index.html` 의 `API_BASE_URL` 을 `http://localhost:3000` 으로 잠깐 바꿔서 브라우저로 열면 됩니다.

## 보안 체크
- ✅ **비밀 키 분리**: `service_role` 키는 Render 환경변수에만. 프론트/깃허브엔 없음.
- ✅ **백엔드 검증**: 글 길이 제한을 서버(`server.js`)와 DB(`CHECK` 제약) 양쪽에서 검사.
- ✅ **XSS 방지**: 사용자 글은 `innerHTML` 이 아니라 `textContent` 로 렌더링.
- ✅ **DB 잠금**: RLS 켜고 정책 없음 → 브라우저 직접 접근 불가, 백엔드만 접근.

## 개선 기능 (과제 제출용)
- **삭제 기능 구현됨**: 각 글의 🗑 버튼 → 백엔드 `DELETE /messages/:id` → DB 삭제.
- 제출 한 줄 예: "글 삭제 기능 추가 (프론트 삭제 버튼 + 백엔드 DELETE 엔드포인트)".
